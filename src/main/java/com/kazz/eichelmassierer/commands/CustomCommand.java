package com.kazz.eichelmassierer.commands;
/*


import com.kazz.eichelmassierer.tracker.GhostTracker;
import net.minecraft.command.CommandBase;
import net.minecraft.command.CommandException;
import net.minecraft.command.ICommandSender;



public class CustomCommand extends CommandBase {
    public boolean ghost = false;

    GhostTracker tracker = new GhostTracker();
    @Override
    public String getCommandName() {
        return "hurensohn";
    }

   // @Override
    //public String getCommandUsage(ICommandSender sender) {
        return "/hurensohn";
    }

/*
    @Override
    public void processCommand(ICommandSender sender, String[] args) throws CommandException {
        tracker.ghost = true;
        System.out.println(tracker.ghost);

    }
}

 */
/*
if (sender instanceof EntityPlayer){
            EntityPlayer player = (EntityPlayer) sender;
            MinecraftForge.EVENT_BUS.register(new GhostTracker());
        }else{
            sender.addChatMessage(new ChatComponentText("Geht nicht du Hurensohn"));
        }
 */
